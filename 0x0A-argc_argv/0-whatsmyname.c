#include "main.h"
#include <stdio.h>

/**
 * main - prints program name
 * @c: number of arguments
 * @v: the argument vector
 *
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
